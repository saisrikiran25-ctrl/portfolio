/**
 * config.example.js — Template for API Configuration
 * PortfolioForge
 *
 * SETUP INSTRUCTIONS:
 *   1. Duplicate this file and rename it to: config.js
 *   2. Replace YOUR_OPENROUTER_API_KEY with your actual key
 *   3. config.js is gitignored — it won't be committed
 *
 * GET YOUR API KEY:
 *   → https://openrouter.ai → API Keys → Create Key
 */

window.OPENROUTER_API_KEY = 'YOUR_OPENROUTER_API_KEY_HERE';
